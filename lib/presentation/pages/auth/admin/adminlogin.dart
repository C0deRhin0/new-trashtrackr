import 'package:flutter/material.dart';

class InitialRegisterPage extends StatelessWidget {
  const InitialRegisterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
